import 'package:flutter/material.dart';
import 'package:rafek_mumen/main.dart';

const double kAllScreenPadding = 16;
const double kWidgetPadding = 12;
const double kRadius = 12;
const double kSmallRadius = 8;
const double kRadius16 = 16;

const double buttomNavIconSize = 30;

double getWidth() {
  return MediaQuery.of(navigatorKey.currentContext!).size.width;
}

double getHeight() {
  return MediaQuery.of(navigatorKey.currentContext!).size.height;
}
